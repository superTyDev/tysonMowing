<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= errorNum %> Error</title>
		<style>
			body {
				background-color: #ef7900;
			}
			.center {
				position: fixed;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				min-width: 50%;

				background-color: white;
				border-radius: 20px;
				padding: 50px;
				box-shadow: 0 20px 50px 10px rgb(20 61 80 / 15%);
			}
			.center h1 {
				border-bottom: 2px solid #ef7900;
			}
			#emailLink {
				color: #ef7900;
				text-decoration: none;
			}
		</style>
	</head>
	<body>
		<main>
			<div class="center">
				<h1>Oops, <%= errorNum %> Error</h1>
				<p><%= errorMessage %></p>
				<p>
					If you believe you have reached this error in *error* you can contact
					the developer at
					<a
						href="znvygb:gl.zpyle@tznvy.pbz"
						onmouseover="reveal(this)"
						id="emailLink"
						>Hover to Reveal</a
					>.
				</p>
			</div>
		</main>
		<script>
			// Caesar Cipher for email
			var alphabet = "abcdefghijklmnopqrstuvwxyz";
			var newalpha = "nopqrstuvwxyzabcdefghijklm";

			function shift(n) {
				// Use shift(13) for same encode and decode function
				for (let i = 0; i < alphabet.length; i++) {
					let offset = (i + n) % alphabet.length;
					newalpha += alphabet[offset];
				}
				return newalpha;
			}

			function encode(message) {
				let result = "";
				message = message.toLowerCase();
				for (let i = 0; i < message.length; i++) {
					if (alphabet.indexOf(message[i]) == -1) {
						result += message[i];
					} else {
						let index = alphabet.indexOf(message[i]);
						result += newalpha[index];
					}
				}
				return result;
			}

			function reveal(elem) {
				if (elem.href.indexOf("mailto:") == -1) {
					elem.href = encode(elem.href);
					elem.textContent = elem.href.split("mailto:")[1];
				}
			}
		</script>
	</body>
</html>
